# 問題集監査レポート（全5領域） - 2026-08-07

実施日: 2026-08-07
対象領域: `basic-operations`（50問） / `feature-usage`（52問） / `prompt-design`（53問） / `security-permissions`（54問） / `token-efficiency`（53問）（計262問、全5領域を`question-bank-update`スキルの`audit`モードで実行）

## 概要

本レポートは `question-bank-update` スキルの `audit` モードを5領域全てに対して実行した結果をまとめたものである。データ修正は含まない。

検査観点は以下の5点。

- 構造の妥当性: id重複（ドメイン内・全ドメイン間）、id命名規約・連番、必須フィールド欠落・空文字、choices要素数、choices重複、correctIndex範囲、level値の妥当性、レベルごとの問題数
- 出題の偏り: 正解選択肢の長さ偏り（1.6倍超/0.625倍未満）、correctIndexの分布偏り、誤答選択肢の断定表現
- 重複出題: 問題文・解説の文字bigram類似度による類似ペア検出（ドメイン内）
- 読解チェック: 正解の複数成立・正解の誤り・問題文と解説の矛盾・レベル配置の妥当性の確認
- 事実照合: 公式ドキュメント（code.claude.com/docs等）との照合

### ドメイン間id重複チェック結果（メインセッション実施）

Node.jsスクリプトで`data/questions/`配下の全5ファイルのidを突合した結果、**ドメイン間の重複id件数は0件**であった。

### 総括（先出し）

5領域すべてにおいて、**「要修正」に該当する指摘はゼロ件**であった。検出された指摘は「検討推奨」および「参考情報」レベルにとどまる。

---

## basic-operations（50問）

### 検査件数・検査観点の要約

50問全件に対し、構造の妥当性・出題の偏り・重複出題（ドメイン内）・読解チェック・事実照合の5観点で検査した。

### 指摘一覧

#### 要修正
なし。

#### 検討推奨

1. **basic-032**: 問題文が「Resume from summary」ダイアログの発動条件を「文脈上限に近づいた」と説明しているが、公式（sessions.md）では「約1時間以上非アクティブかつ10万トークン超」が正しい条件。選択肢・正解自体は公式記述と一致しており、問題文の前置き表現のみの軽微な指摘。
   - 根拠URL: https://code.claude.com/docs/en/sessions

#### 参考情報

1. **正解選択肢の長さ偏り**（比率1.6倍超/0.625倍未満）: `basic-001`(0.46)、`basic-023`(0.53)、`basic-030`(1.65)、`basic-034`(1.99)、`basic-035`(2.31)、`basic-036`(1.85)、`basic-037`(2.23)、`basic-038`(1.98)、`basic-039`(1.76)、`basic-041`(1.84)、`basic-046`(1.92)
2. **correctIndexの分布**: 各レベル区分で最大36〜43%、50%閾値超なし。問題なし。
3. **誤答選択肢の断定表現**: `basic-007`、`024`、`029`、`032`、`035`、`036`、`045`、`046`、`047`、`050`の計10件。文脈上意図的な配置と判断。

#### 指摘がない観点

- 構造の妥当性: 50問全件パス
- 重複出題（ドメイン内）: 閾値超ペアゼロ
- 読解チェック: 正解の一意性等、問題なし
- 事実照合: `basic-032`を除き全て公式ドキュメントと一致

---

## feature-usage（52問）

### 検査件数・検査観点の要約

52問全件に対し、構造の妥当性・出題の偏り・重複出題（ドメイン内）・読解チェック・事実照合の5観点で検査した。

### 指摘一覧

#### 要修正
なし。

#### 検討推奨

1. **正解選択肢の長さ偏り**
   - `feature-006`: 比率1.70倍
   - `feature-012`: 比率1.75倍
   - `feature-040`: 比率1.98倍（最大）
   - `feature-041`: 比率0.60倍（正解が短すぎる方向の偏り）
   - `feature-044`: 比率1.92倍
   - `feature-050`: 比率1.89倍
   - `feature-052`: 比率1.87倍
2. **correctIndexの分布**: advancedでインデックス0が43.75%、expertでインデックス1が46.2%。50%閾値未満だがやや偏りあり。

#### 参考情報

1. **誤答選択肢の断定表現**: `feature-014`、`027`、`031`、`033`、`036`、`040`、`046`
2. **重複出題候補（ドメイン内）**: 機械検出の最大類似度でも問題文0.42・解説0.37に留まり、内容確認の結果実質的な重複なし
3. **feature-038**: MCPサーバー重複解決順位の説明が「local>project>user>plugin」の4段階のみで、公式の5段階目「claude.ai connectors」が抜けている。正解の趣旨自体は事実と矛盾しない。
   - 根拠: MCPサーバー設定の公式ドキュメント（優先順位に関する記述）

#### 指摘がない観点

- 構造の妥当性: 全項目パス（id重複なし・各レベル10問以上）
- 読解チェック: 矛盾等なし
- 事実照合: `feature-038`の軽微な指摘を除き、hooks/mcp/sub-agents/statusline/output-styles/agent-teams/interactive-modeの公式ドキュメントと一致確認済み。特に`feature-042`の`anthropics/claude-code-action@v1`の実在も確認済み。

---

## prompt-design（53問）

### 検査件数・検査観点の要約

53問全件に対し、構造の妥当性・出題の偏り・重複出題（ドメイン内）・読解チェック・事実照合の5観点で検査した。

### 指摘一覧

#### 要修正
なし。

#### 検討推奨

1. **prompt-002 / prompt-018 / prompt-031**: 「曖昧な指示 vs 具体的指示によるファイルスキャン範囲の違い」という同一知識の3重出題疑い。beginner×2、advanced×1にまたがる。
2. **prompt-006 / prompt-017**: 「CLAUDE.mdの自動読込タイミング」という同一事実の重複出題疑い（intermediate/beginner）。
3. **prompt-039**: explanationの「agent teamsのplan modeは通常セッションの約7倍のトークンを使う」という数値が公式ドキュメント（agent-teams.md）に見当たらない。定性的記述のみ存在。正解自体は妥当。
   - 根拠URL: agent-teams.md（該当数値は未確認）
4. **prompt-051**: explanationの「8回連続でブロックされるとフックを上書きしてターンを終了する」という具体的回数が公式ドキュメント（goal.md）に見当たらない。
   - 根拠URL: goal.md（該当回数は未確認）
5. **prompt-008**: 正解選択肢の長さ偏り比率0.58倍
6. **prompt-017**: 正解選択肢の長さ偏り比率0.41倍。誤答が全て否定文、正解のみ肯定文という型の違いもあり。
7. **prompt-053**: 正解選択肢の長さ偏り比率1.63倍
8. **prompt-048**: `AskUserQuestion`というツール名が公式ツール一覧（sdk-tools.md等）で確認できなかった。設問の主旨（インタビュー形式での仕様確定）自体は一般的プラクティスとして成立。ツール名の実在性のみ要確認。

#### 参考情報

1. **誤答選択肢の断定表現**: `prompt-004`、`007`、`014`、`015`、`016`、`018`、`020`、`023`、`026`、`027`、`028`、`032`、`033`、`035`、`040`、`043`、`048`、`050`、`051`、`052`、`053`など20問以上（ドメイン全体の傾向、意図的な出題スタイルの可能性）
2. **prompt-011 / prompt-022**: bigram類似度0.27〜0.28とやや高いが、内容確認の結果、問う対象が異なり重複ではないと判断
3. **prompt-050**: "infinite exploration"等の用語について、対になる用語の一次資料までは確認しきれていないが、出題の核心は公式ブログの論調と整合的

#### 指摘がない観点

- 構造の妥当性: id重複・欠番なし、各レベル10問以上
- 読解チェック: 矛盾等なし（`prompt-002`/`018`/`031`の重複出題観点を除く）
- 事実照合: CLAUDE.md自動読込、SKILL.mdのdescription、disable-model-invocation、`.claude/rules/`のpaths、@インポートの4階層制限、CLAUDE.mdの200行目安、claudeMdExcludes設定、managed policy配下のCLAUDE.mdパス、Routinesの自律実行、Explore→Plan→Implement→Commitワークフロー、"lost in the middle"、"right altitude"は公式情報源で確認済み

---

## security-permissions（54問）

### 検査件数・検査観点の要約

54問全件に対し、構造の妥当性・出題の偏り・重複出題（ドメイン内）・読解チェック・事実照合の5観点で検査した。

### 指摘一覧

#### 要修正
なし。

#### 検討推奨
なし。

#### 参考情報

1. **誤答選択肢の極端な断定表現**: `security-003`、`006`、`008`、`009`、`010`、`012`、`013`、`015`、`026`、`035`、`037`、`042`、`043`、`044`、`045`、`047`、`052`、`053`、`054`の計19問（選択肢ベースで22箇所）。全体の約35%とやや多めだが意図的設計の可能性。
2. **正解選択肢の長さ偏り**: `security-007`(1.64)、`security-020`(0.61)、`security-034`(1.61)、`security-043`(1.77)、`security-045`(1.90)、`security-054`(2.22、最大)
3. **correctIndexの分布**: beginnerでインデックス3が40%、intermediateでインデックス0が46%、expertでインデックス2が41%。いずれも50%閾値未満。

#### 指摘がない観点

- 構造の妥当性: 全54問パス、id連番・重複なし、各レベル10問以上
- 重複出題（ドメイン内）: 機械検出7ペアは全て意図的な対構成と判断
- 読解チェック: 矛盾等なし。`security-023`の解説がやや簡略化されているが正誤判定に影響なし
- 事実照合: 28問について公式ドキュメント（permission-modes/permissions/hooks/sandboxing）、およびAnthropic Engineering Blogと照合し全て一致。`security-022`の`Bash(npm run test:*)`表記、`security-038`のparam:valueマッチング対象外の主張も確認済み

---

## token-efficiency（53問）

### 検査件数・検査観点の要約

53問全件に対し、構造の妥当性・出題の偏り・重複出題（ドメイン内）・読解チェック・事実照合の5観点で検査した。

### 指摘一覧

#### 要修正
なし。

#### 検討推奨

1. **token-039**: 「effort limits」の説明が「組織の管理者が最大effortレベルを設定できる」とだけ説明しているが、公式（model-config#organization-effort-limits）では「Claude Enterpriseプランの管理者がcustom roleごとに設定」「organization model restrictionsと一緒に提供」「Bedrock/Google Cloud/Microsoft Foundry等では非対応」という限定条件がある。
   - 根拠URL: model-config#organization-effort-limits
2. **token-021 / token-023**: TTL説明で「サブスクリプションでは1時間TTL」としているが、公式（code.claude.com/docs/en/costs）では「usage creditsを使い始めると5分に低下する」という条件が抜けている。`token-022`の解説には条件が明記されており、ドメイン内で整合はしているが`token-021`単独では誤解を招きうる。
   - 根拠URL: https://code.claude.com/docs/en/costs

#### 参考情報

1. **レベル配分**: beginner10/intermediate12/advanced17/expert14。advanced/expertに問題がやや集中（10問以上の基準は満たす）
2. **correctIndexの分布**: expertレベルでインデックス3が0件（他は4,4,6件）。50%閾値（42.9%）は超えない
3. **「〜のみ」という限定表現を含む誤答選択肢が多い**: `token-003`、`018`、`021`、`027`、`031`×2、`033`×2、`043`×2、`044`、`050`×2。極端な断定表現とまでは言えない
4. **選択肢の文字数比率がやや偏る**: `token-001`(0.60)、`token-025`(0.58)、`token-042`(1.61)、`token-043`(1.63)。いずれも軽微
5. **問題文の類似度が高いペア**（`token-002`/`028`等）は機械検出のみで、内容確認の結果意図的な対比構成であり重複ではない

#### 指摘がない観点

- 構造の妥当性: 全53問パス、id重複・欠番なし、各レベル10問以上
- 重複出題（ドメイン内）: 実質的な重複なし
- 読解チェック: 矛盾等なし
- 事実照合: TTL・キャッシュ階層構造・最小キャッシュトークン数・Agent teamsの7倍という数値・エンタープライズ平均コスト・effortレベルの永続化仕様・opusplanの動作・スケジュールタスクの挙動について、`token-039`・`token-021`/`023`を除き公式ドキュメントと一致確認済み

---

## 全体総括

5領域（basic-operations 50問 / feature-usage 52問 / prompt-design 53問 / security-permissions 54問 / token-efficiency 53問、計262問）を対象に`question-bank-update`スキルの`audit`モードを実行した結果、**5領域とも「要修正」レベルの構造的欠陥・事実誤認は検出されなかった**。ドメイン間のid重複チェックも0件であり、データ全体の整合性に問題はない。

主な指摘は以下の2種に大別される。

- **検討推奨レベル**: 選択肢長さの偏り（正解選択肢が他より1.6倍超長い、または0.625倍未満に短い問題が各領域に散見）、軽微な重複トピック（`prompt-002`/`018`/`031`、`prompt-006`/`017`）、explanation中の出典不明な具体的数値（`prompt-039`の「agent teamsのplan modeは通常セッションの約7倍のトークンを使う」、`prompt-051`の「Stop hookが8回連続でブロックされるとフックを上書きしてターンを終了する」など）
- **参考情報レベル**: 誤答選択肢における断定的言い回し（「一切」「絶対に」「〜のみ」等）の多用、correctIndexのレベル内分布のやや偏った傾向（いずれも50%閾値は超えない）、機械検出による類似ペアの多くが内容確認の結果は意図的な対構成であったこと

いずれも即時修正が必要な事実誤認ではなく、次回の新規問題追加・改訂時に留意すべき事項として記録する。本監査を通じて`data/questions/*.json`への変更は一切行っていない。
